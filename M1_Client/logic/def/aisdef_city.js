if(window.aisEnv.defLib && window.aisEnv.defLib.city&& !window.aisEnv.defLib.city.inited)
{
	//建筑的DEF:
	(function()
	{
		var defLib=window.aisEnv.defLib.city;
		defLib.inited=1;
		//数值
		defLib["values"]=[
		];
	})();
}